spring:
  application:
    name: secure-ticketing-api

  datasource:
    url: jdbc:h2:mem:ticketing;MODE=PostgreSQL;DATABASE_TO_LOWER=TRUE;DB_CLOSE_DELAY=-1

  jpa:
    open-in-view: false
    hibernate:
      ddl-auto: validate

server:
  port: 0
